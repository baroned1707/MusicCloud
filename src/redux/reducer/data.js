import {} from "../action";

const defaultStore = {
  createBy: "BaronED",
};

const data = (state = defaultStore, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default data;
